//function calcFatorial(){
    let n = window.prompt("Digite o valor de N!");
    parseInt(n);
    let resultado = n;
    while(n > 1){
        resultado = resultado*(n-1);n--;
    } 
    alert("O resultado do calculo fatorial é " + resultado);
}

function nFatorial(n){
    let n = window.prompt("Digite o valor de N!");
    parseInt(n);
};

function calcFatorial(nFatorial()){
    
};
